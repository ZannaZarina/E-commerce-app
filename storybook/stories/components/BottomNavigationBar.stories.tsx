import React from 'react';
import { storiesOf } from '@storybook/react-native';
import { BottomNavigationBar } from '../../../src/components';

storiesOf('BottomNavigationBar', module)
.add('default', () => 
    <BottomNavigationBar/> )
