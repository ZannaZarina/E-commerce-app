import React from 'react';
import { storiesOf } from '@storybook/react-native';
import { Success } from '../../../src/views';

storiesOf('Success', module)
    .add('default', () => 
        <Success /> );